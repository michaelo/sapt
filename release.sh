# Input: version (start with num)
# Update VERSION
# Build native and run tests
# If successful: run all crossbuilds and grenerate archives
# If successful: stage VERSION, commit and tag
# If successful: TBD: push, create github-release, upload artifacts
#                If verison is "x.y.z", it's a proper release, otherwise a prerelease